/**
 * Platform 모듈 — 공통 코드·메뉴·권한(role).
 *
 * <p>테이블 prefix: {@code pl_*}
 * <p>주요 엔터티: {@code pl_code_group}, {@code pl_code}, {@code pl_menu}, {@code pl_role}, {@code pl_member_role}
 *
 * <p>의존: 없음 (가장 하단 모듈). 다른 모든 모듈이 참조 가능.
 */
package com.hopenvision.platform;
