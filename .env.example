# ==============================================
# HopenVision Environment Configuration
# ==============================================
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.
# cp .env.example .env

# Database Configuration (shared PostgreSQL)
DB_HOST=postgresql
DB_PORT=5432
DB_NAME=hopenvision
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# Spring Boot
SPRING_PROFILES_ACTIVE=dev

# CORS (쉼표로 구분)
CORS_ALLOWED_ORIGINS=http://localhost:4060,http://localhost:5173,http://localhost:3000

# Timezone
TZ=Asia/Seoul
